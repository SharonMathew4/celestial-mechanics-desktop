<UserControl x:Class="CelestialMechanics.Desktop.Views.Shell.ObservationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0"
                HorizontalAlignment="Left"
                Padding="12,6"
                Content="Refresh Horizons"
                Command="{Binding RefreshCommand}" />

        <TextBlock Grid.Row="1"
                   Margin="0,16,0,0"
                   Text="{Binding Status}"
                   TextWrapping="Wrap" />
    </Grid>
</UserControl>
